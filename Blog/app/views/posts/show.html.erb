<h1><%= @post.title %></h1>

<h2><%= @post.author %></h2>

<p><%= @post.content %></p>

<% @comments.each do |comment| %>
	<p> + <%= comment.comment %></p>
<% end %>


<%= render 'comments/form' %>

<br>
<%= link_to "<<Back", posts_path %>